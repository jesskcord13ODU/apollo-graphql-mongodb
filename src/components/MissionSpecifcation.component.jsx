import React from 'react';
import { useEffect } from 'react';

export const MissionSpecification = ({ mission }) => {
    const [Specs, setSpecs] = useState();

    return (
        <>{ Specs }</>
    );
}
